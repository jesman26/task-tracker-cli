{
  "name": "task-tracker-cli",
  "version": "1.0.0",
  "description": "A command-line interface for managing tasks using a JSON file for storage.",
  "main": "src/index.ts",
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "tsc"
  },
  "dependencies": {
    "ts-node": "^10.0.0",
    "typescript": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^14.0.0"
  },
  "license": "MIT"
}